apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi  # Match the storage capacity with your PV

