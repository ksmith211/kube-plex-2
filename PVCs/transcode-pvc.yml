apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transcode-pvc2
  namespace: plex
spec:
  storageClassName: vsphere-ssd1
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi  # Match the storage capacity with your PV

